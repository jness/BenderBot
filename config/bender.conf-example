[IRC]
server = irc.freenode.net
port = 6667
channel = #bender-test
botnick = benderbot
nickmsg = Bite my shiny metal ass

# If you need to identify to the IRC
# server uncomment the below
# password = mypassword

# Some IRC servers such as Freenode need a couple seconds
# on connection before they can join the channel.
wait = 10

[RabbitMQ]
host = 127.0.0.1
port = 5672

# If you wish to use RabbitMQ PlainAuth
# username = admin
# password = password

exchange = irc
